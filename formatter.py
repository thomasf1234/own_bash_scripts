import subprocess

class Formatter:
   #raw_data = {'service1': 'results1, 'service2': 'results2', 'service3': 'results3'}
   def __init__(self, raw_data):
       self.raw_data = raw_data
   
   def 
   result = subprocess.check_output('grep --color -ir service *', shell=True) #try to bypass this by filtering the directories to search before hand
   print result
        
